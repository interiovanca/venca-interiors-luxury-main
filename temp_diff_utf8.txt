diff --git a/src/components/FloatingThemeToggle.tsx b/src/components/FloatingThemeToggle.tsx
index a380560..8f468be 100644
--- a/src/components/FloatingThemeToggle.tsx
+++ b/src/components/FloatingThemeToggle.tsx
@@ -70,7 +70,7 @@ const FloatingThemeToggle = ({ isVisible }: FloatingThemeToggleProps) => {
               whileHover={{ scale: 1.05 }}
               whileTap={{ scale: 0.95 }}
               onClick={toggleTheme}
-              className="w-10 h-10 rounded-full bg-card/90 hover:bg-card border border-border/50 shadow-lg backdrop-blur-sm flex items-center justify-center transition-all duration-300 group"
+              className="w-10 h-10 rounded-full bg-white/90 hover:bg-card border border-border/50 shadow-lg backdrop-blur-sm flex items-center justify-center transition-all duration-300 group"
               aria-label={`Switch to ${theme === 'dark' ? 'light' : 'dark'} mode`}
             >
               <motion.div
